import { ToDoList } from "../../pages/toDoList";
/**
 * Body Component
 * 
 * Represents the main content of the application body.
 * In this case, it renders the ToDoList page.
 * 
 * @returns {JSX.Element} - The JSX element representing the body content.
 */
export const Body = () => {
    return (
        <ToDoList />
    );
};